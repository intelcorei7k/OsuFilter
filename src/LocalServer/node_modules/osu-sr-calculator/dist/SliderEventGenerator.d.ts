import { SliderEventDescriptor } from "./Objects/osu!/HitObjects/SliderEventDescriptor";
declare class SliderEventGenerator {
    static Generate(startTime: number, spanDuration: number, velocity: number, tickDistance: number, totalDistance: number, spanCount: number, legacyLastTickOffset?: number): SliderEventDescriptor[];
    private static generateTicks;
}
export default SliderEventGenerator;
