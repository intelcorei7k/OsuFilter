declare class Utils {
    static initializeArray(length: number, initialValue?: any): any[];
    static PowerMeanValues(x: number, y: number, i: number): number;
    static PowerMean(values: number[], i: number): number;
}
export default Utils;
