"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class Utils {
    static initializeArray(length, initialValue = 0) {
        const array = [];
        for (let i = 0; i < length; i++) {
            if (Array.isArray(initialValue))
                array.push(JSON.parse(JSON.stringify(initialValue)));
            else
                array.push(initialValue);
        }
        return array;
    }
    static PowerMeanValues(x, y, i) {
        return Math.pow((Math.pow(x, i) + Math.pow(y, i)) / 2, 1 / i);
    }
    static PowerMean(values, i) {
        let sum = 0;
        let count = 0;
        values.forEach(x => {
            sum += Math.pow(x, i);
            count++;
        });
        return Math.pow(sum / count, 1 / i);
    }
}
exports.default = Utils;
